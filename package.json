{
  "name": "jeva",
  "version": "1.3.3",
  "description": "Safely evalulate JavaScript",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --format=cjs --outfile=build/index.js && esbuild index.js --bundle --minify --sourcemap --format=iife --global-name=jeva --outfile=build/jeva.min.js",
    "dev": "esbuild index.js --bundle --format=cjs --outfile=build/index.js --watch",
    "test": "tape test/*.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/expreva/jeva"
  },
  "keywords": [
    "eval",
    "sandbox",
    "esprima",
    "safe",
    "expression"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/expreva/jeva/issues"
  },
  "dependencies": {
    "esprima": "^4.0"
  },
  "devDependencies": {
    "esbuild": "^0.12.5",
    "tape": "4"
  }
}
