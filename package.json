{
  "name": "@expreva/jeva",
  "version": "1.4.0",
  "description": "Safely evalulate JavaScript",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --minify --sourcemap --format=iife --global-name=jeva --outfile=build/jeva.min.js",
    "dev": "esbuild index.js --bundle --sourcemap --format=iife --global-name=jeva --outfile=build/jeva.min.js --watch",
    "test": "tape test/*.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/expreva/jeva"
  },
  "keywords": [
    "eval",
    "sandbox",
    "esprima",
    "safe",
    "expression"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/expreva/jeva/issues"
  },
  "dependencies": {
    "meriyah": "^4.1.5"
  },
  "devDependencies": {
    "esbuild": "^0.12.5",
    "tape": "4"
  }
}
